# This file keeps the certs/ directory tracked by Git.
# Actual certificate files (*.crt, *.key, *.pfx, *.pem) are excluded via .gitignore.
# Run docker/nginx/generate-certs.sh (or generate-certs.ps1 on Windows) to generate them.
